language: c

matrix:
  include:
    - os: linux
      sudo: false

compiler:
  - gcc
  - clang

addons:
  apt:
    sources:
      - george-edison55-precise-backports
    packages:
      - cmake-data
      - cmake

script: 
  - mkdir build
  - cd build 
  - cmake .. 
  - make
  - make test

